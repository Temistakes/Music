export const getTracks = (state) => state.tracks.items;
export const getLimit = (state) => state.tracks.limit;
export const getPage = (state) => state.tracks.page;
export const getTotalResults = (state) => state.tracks.total;
export const getOrder = (state) => state.tracks.order;